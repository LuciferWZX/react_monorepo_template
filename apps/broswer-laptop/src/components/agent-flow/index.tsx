import { Background, Controls, ReactFlow } from "@xyflow/react";

const AgentFlow = () => {
  return (
    <ReactFlow>
      <Background />
      <Controls />
    </ReactFlow>
  );
};
export default AgentFlow;
